import 'package:flutter/material.dart';

class FatherDashboard extends StatelessWidget {
  const FatherDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Father Dashboard'),
      ),
    );
  }
}
